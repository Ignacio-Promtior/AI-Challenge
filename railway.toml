[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Server starts immediately so healthcheck passes fast.
# 30 min window covers model download + scraping + ingest on first boot.
healthcheckPath = "/"
healthcheckTimeout = 1800
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
