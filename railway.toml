[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# LLaMA 2 download + scraping + ingest on first boot can take 10-15 min
healthcheckPath = "/"
healthcheckTimeout = 900
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
